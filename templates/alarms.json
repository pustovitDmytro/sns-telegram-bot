{
    "default": "Amazon CloudWatch Alarm <b>{{AlarmName}}</b> in the {{Region}} region for account {{AWSAccountId}} has entered the <i>{{NewStateValue}}</i> state, because '{{NewStateReason}}' at {{datetime StateChangeTime}}"
}